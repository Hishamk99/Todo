class TaskModel {
  final String title;
  final String description;

  TaskModel({
    required this.title,
    required this.description,
  });

  // factory TaskModel.fronJson(jsonData) {
  //   return TaskModel(title: ,
  //    description:
  //   );
  // }
}
